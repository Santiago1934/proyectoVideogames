/*const axios = require ("axios");
require('dotenv').config();
const {API_KEY} = process.env;

const Search = (req, res) => {
    axios.get(`https://api.rawg.io/api/games/s?&&key=${API_KEY}`)
    .then( data => data)
    .then(response => res.send(response.data))
}

module.exports= Search;*/